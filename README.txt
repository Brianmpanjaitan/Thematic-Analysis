To install the necessary packages, run the following lines in the terminal one at a time:
pip install pandas
pip install numpy
pip install scikit-learn
pip install nltk
pip install contractions
pip install pyodbc
pip install spacy
python -m spacy download en_core_web_sm
